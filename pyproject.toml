[tool.black]
line-length = 100
target-version = ["py310"]
extend-exclude = '''
/(
  venv
)/
'''

[tool.isort]
profile = "black"
line_length = 100
py_version = 310
skip = ["venv"]

[tool.mypy]
python_version = "3.10"
files = ["app"]
exclude = ["venv"]
